stages:
    - build
    - validate
    
build_tf_container:
    stage: build
    image: docker
    services:
        - docker:dind
    before_script:
        - docker login registry.gitlab.com -u mateusz.lewicki -p 9YS9pwVMNNdEkz2kmx71
    script: 
        - docker build -t registry.example.com/private/AWS_POC/terraform .
        - docker push registry.example.com/private/AWS_POC/terraform
job:
    stage: validate
    image: registry.example.com/private/AWS_POC/terraform:latest
    script: terraform validate ./Terraform
